# 🚀 Инструкция по настройке Supabase

## Ваш проект Supabase
**URL**: `https://rnxqyltjbcwqwblnhuhm.supabase.co`  
**Dashboard**: https://supabase.com/dashboard/project/rnxqyltjbcwqwblnhuhm

## 📋 Шаги настройки

### Шаг 1: Выполните SQL скрипт

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в **SQL Editor** (левое меню)
3. Нажмите **New Query**
4. Откройте файл `scripts/setup-supabase-complete.sql` из проекта
5. Скопируйте **весь** SQL код
6. Вставьте в SQL Editor
7. Нажмите **Run** (или Ctrl+Enter)
8. Должно появиться сообщение об успешном выполнении

### Шаг 2: Проверьте создание таблиц

1. В SQL Editor выполните скрипт `scripts/verify-supabase.sql`
2. Или вручную выполните:
   ```sql
   SELECT * FROM users LIMIT 1;
   SELECT * FROM progress LIMIT 1;
   ```

### Шаг 3: Проверьте подключение из кода

```bash
# Запустите тест
node scripts/test-connection.js
```

Должно вывести:
- ✅ Подключение к Supabase работает!
- ✅ Таблица users доступна
- ✅ Таблица progress доступна

### Шаг 4: Запустите приложение

```bash
npm run dev
```

Откройте консоль браузера (F12) и проверьте:
- Должно быть: `✅ Supabase успешно подключена: https://rnxqyltjbcwqwblnhuhm.supabase.co`

### Шаг 5: Проверьте работу

1. **Войдите через Telegram** - данные должны загрузиться
2. **Завершите задачу** - в консоли должно быть: `✅ Task completed and synced to Supabase`
3. **Купите предмет** - в консоли должно быть: `✅ Purchase synced to Supabase`
4. **Обновите страницу** - прогресс должен сохраниться

## 🔍 Проверка в Supabase Dashboard

### Проверка данных

1. Перейдите в **Table Editor**
2. Должны быть таблицы: `users` и `progress`
3. При создании пользователя через приложение, он должен появиться в таблице `users`
4. При завершении задачи, запись должна появиться в таблице `progress`

### Проверка RLS (Row Level Security)

1. Перейдите в **Authentication** → **Policies**
2. Для таблиц `users` и `progress` должны быть политики:
   - "Public users access" (для users)
   - "Public progress access" (для progress)

## 🐛 Решение проблем

### Ошибка: "relation 'users' does not exist"
**Решение**: Выполните SQL скрипт `setup-supabase-complete.sql`

### Ошибка: "new row violates row-level security policy"
**Решение**: 
1. Проверьте что политики RLS созданы
2. Выполните в SQL Editor:
   ```sql
   SELECT * FROM pg_policies WHERE tablename IN ('users', 'progress');
   ```

### Данные не сохраняются
**Проверьте**:
1. Консоль браузера на ошибки
2. Что таблицы созданы (Table Editor)
3. Что RLS политики настроены
4. Что ключи правильные в `services/supabaseClient.ts`

### Пользователь не находится при входе
**Проверьте**:
1. Консоль браузера: `DB: Checking Supabase for Telegram ID...`
2. Что `telegram_id` правильно сохраняется в БД
3. Выполните в SQL Editor:
   ```sql
   SELECT id, telegram_id, name FROM users;
   ```

## 📊 Структура таблиц

### Таблица `users`
- `id` (UUID) - уникальный ID пользователя
- `telegram_id` (BIGINT) - ID из Telegram (уникальный)
- `name`, `username` - данные пользователя
- `xp`, `coins`, `level` - игровые данные
- `hp`, `max_hp` - здоровье
- `inventory` (JSONB) - массив ID купленных предметов
- `completedTaskIds` - **НЕ хранится напрямую**, загружается из таблицы `progress`

### Таблица `progress`
- `id` (UUID) - уникальный ID записи
- `user_id` (UUID) - ссылка на `users.id`
- `task_id` (TEXT) - ID завершенной задачи (например: 't1', 't2')
- `xp_earned` - полученный XP за задачу
- `completed_at` - дата и время завершения

## ✅ Чеклист настройки

- [ ] SQL скрипт выполнен в Supabase Dashboard
- [ ] Таблицы `users` и `progress` созданы
- [ ] Индексы созданы
- [ ] RLS политики настроены
- [ ] Тест подключения прошел успешно
- [ ] Приложение запускается без ошибок
- [ ] Данные сохраняются при завершении задач
- [ ] Данные сохраняются при покупке предметов
- [ ] Прогресс сохраняется после перезагрузки

## 🎯 Готово!

После выполнения всех шагов ваше приложение будет полностью синхронизироваться с Supabase!
