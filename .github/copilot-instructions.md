<!-- Инструкции для AI-агентов (Copilot, Claude и др.) -->
# AI Teenager — Гайд для AI-агентов

Краткое руководство, чтобы AI-агент мог сразу работать с этим репозиторием.

## ⚠️ КРИТИЧЕСКОЕ ОГРАНИЧЕНИЕ: SOLO USER

> **В приложении занимается ОДИН подросток. Никаких групповых механик!**

| ❌ ЗАПРЕЩЕНО | ✅ РАЗРЕШЕНО |
|--------------|--------------|
| Групповые игры | Solo-игры с AI |
| Соревнования с другими | Соревнования с самим собой (личные рекорды) |
| Парная работа | Диалог с AI-ассистентом Катей |
| Лидерборды | Личный прогресс и стрики |
| Командные квесты | Индивидуальные квесты |

**Катя (AI) заменяет**: учителя, партнера, группу поддержки.

---

## 1) Общая картина
- **Стек**: React 19 + TypeScript + Vite. Стили через Tailwind CSS (CDN).
- **AI**: клиентский, через `@google/genai` в `services/geminiService.ts`. Бэкенда нет.
- **Роли пользователей**: `TEEN` (подросток), `PARENT` (родитель), `CURATOR` (куратор).
- Переключение ролей в `App.tsx` через `RoleSelector`, рендерит разные дашборды:
  - `components/TeenDashboard.tsx` — квесты, XP, чат с Катей
  - `components/ParentDashboard.tsx` — лекции для родителей
  - `components/CuratorDashboard.tsx` — CRM для куратора

## 2) Ключевые файлы (что где искать)
| Файл | Назначение |
|------|------------|
| `services/geminiService.ts` | **Мозг AI** — системные промпты, вызовы Gemini. Меняй `systemPrompt` для изменения личности Кати |
| `constants.ts` | **Данные** — `TASKS`, `MOCK_USER`, `KATYA_VARIANTS`, `LECTURES`. Добавляй квесты сюда |
| `components/KatyaChat.tsx` | **Чат UI** — анимации персонажа (IDLE, BLINK, TALK), вызов `askKatya()` |
| `components/TeenDashboard.tsx` | **Главный экран** — SVG-дорожка квестов, прогресс |
| `types.ts` | **Типы** — `User`, `Task`, `ChatMessage`. Следуй им при добавлении фич |
| `save_data.json` | **Прогресс** — XP, пройденные квесты, история чата. Коммитится через Cloud Sync |
| `vite.config.ts` | **Конфиг** — маппинг `GEMINI_API_KEY` в `process.env.*` |

## 3) Как запустить локально
```bash
npm install
echo "GEMINI_API_KEY=твой_ключ" > .env.local
npm run dev        # dev-сервер на порту 3000
npm run build      # production сборка в dist/
```

## 4) Важные правила
- **Язык**: весь UI и промпты на **русском языке**. Не переводи на английский.
- **Секреты**: НИКОГДА не коммить API-ключи. Используй `.env.local` (в .gitignore).
- **Стейт**: локальный через `useState`. Глобальный стейт поднимается в `App.tsx`.
- **Tailwind**: загружается через CDN в `index.html`, PostCSS не используется.
- **Иконки**: используй `lucide-react`.

## 5) Cloud Sync (сохранение прогресса)
- `save_data.json` коммитится в репо через GitHub Actions (кнопка "Сохранить прогресс" в AI Studio)
- **НЕ добавляй** этот файл в `.gitignore` — он нужен для облачной синхронизации
- Для локальной разработки без Cloud Sync используй `localStorage`

## 6) Частые задачи
| Задача | Что делать |
|--------|-----------|
| Изменить личность Кати | Редактируй `systemPrompt` в `services/geminiService.ts` |
| Добавить новый квест | Добавь объект в `TASKS` в `constants.ts` |
| Изменить анимации Кати | Правь `KATYA_VARIANTS` в `constants.ts` |
| Добавить новую роль | Редактируй `App.tsx` и создай компонент в `components/` |

## 7) Правила коммитов (для AI Studio)
При сохранении прогресса:
- **Коммить ВСЕ изменённые файлы**, не только `save_data.json`
- Формат сообщения: `<тип>: Краткое описание` (типы: feat, fix, style, docs, refactor)
- Группируй связанные изменения в один коммит
- ✅ Хорошо: `feat: Добавлена PRO игра ThermiteGrid с комбо-системой`
- ❌ Плохо: `update` или `💾 Auto-save`

## 8) Отладка
- **Белый экран** → смотри консоль браузера (F12)
- **AI не отвечает** → проверь `GEMINI_API_KEY` в `.env.local` и перезапусти `npm run dev`
- **WebSocket ошибки** → открой в обычном браузере, не в Simple Browser VS Code

## 9) Не меняй без проверки
- `vite.config.ts` — маппинг env-переменных влияет на весь билд
- URL-адреса ассетов в `constants.ts` — используются во многих компонентах

---
*Этот файл помогает AI-агентам быстро ориентироваться в проекте.*
