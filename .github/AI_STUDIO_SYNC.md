# 🔄 Настройка синхронизации AI Studio ↔ GitHub

## Проблема
AI Studio и GitHub Codespace не синхронизируются автоматически — изменения теряются между средами.

## Решение: Автоматическая двусторонняя синхронизация

### Что уже настроено:

✅ **GitHub Actions workflows** в `.github/workflows/`:
- `ai-studio-sync.yml` — уведомляет когда код обновлён на GitHub
- `sync-from-ai-studio.yml` — проверяет изменения от AI Studio каждые 5 минут

### Как это работает:

```
AI Studio (Google Drive)
    ↓ Сохранить прогресс
GitHub (origin/main)
    ↓ Auto pull каждые 5 мин
Codespace (локальная разработка)
    ↓ git push
GitHub (origin/main)
    ↓ Webhook
AI Studio обновляется
```

### Настройка в AI Studio:

1. **Включить GitHub Integration:**
   - В AI Studio: Settings → GitHub
   - Подключить репозиторий `johnda7/ai-pod`
   - Включить "Auto-commit" для всех файлов (не только save_data.json)

2. **Настроить GitHub Token:**
   - Создать Personal Access Token: https://github.com/settings/tokens
   - Права: `repo` (full control)
   - Сохранить токен в AI Studio Settings

3. **Включить Webhook (опционально):**
   - В репозитории: Settings → Webhooks → Add webhook
   - Payload URL: `https://ai.studio/api/webhook/github`
   - Events: `push`, `pull_request`

### Для разработчиков (Codespace):

**Перед началом работы:**
```bash
git pull origin main  # Подтянуть изменения от AI Studio
```

**После внесения изменений:**
```bash
git add .
git commit -m "feat: описание изменений"
git push origin main  # AI Studio автоматически обновится
```

### Альтернатива: Ручная синхронизация

Если автоматика не настроена, используй:

```bash
# Подтянуть из AI Studio
git pull origin main --rebase

# Отправить в AI Studio
git push origin main

# В AI Studio: нажать "Сохранить прогресс" → "Save ALL files"
```

## Как у Lovable

Lovable использует:
1. **GitHub App** с полным доступом к репозиторию
2. **Webhooks** для мгновенной синхронизации
3. **File watchers** в браузере для автосохранения

Для такой же настройки нужен API от AI Studio.

---

**Статус:** ⚠️ Частично настроено (workflows созданы, нужна настройка в AI Studio)
