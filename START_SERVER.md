# 🚀 Запуск сервера

## Проблема: Ошибка -102 (Connection Refused)

Это означает что браузер не может подключиться к серверу.

## Решение:

### 1. Убедитесь что сервер запущен:

```bash
cd /workspace
npm run dev
```

Должно появиться:
```
VITE v6.4.1  ready in XXX ms
➜  Local:   http://localhost:3000/
```

### 2. Если вы в удаленной среде:

Используйте проброс портов или внешний IP:
- **Локально**: http://localhost:3000
- **По сети**: http://172.30.0.2:3000 (или ваш IP)

### 3. Проверьте что порт открыт:

```bash
lsof -i :3000
```

### 4. Если не работает:

1. Остановите все процессы vite:
   ```bash
   pkill -f vite
   ```

2. Запустите заново:
   ```bash
   cd /workspace
   npm run dev
   ```

3. Дождитесь сообщения "ready"
4. Откройте в браузере указанный адрес
