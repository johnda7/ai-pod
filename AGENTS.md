# AI Teenager — Гайд для AI-агентов

## 🎯 О проекте

**AI Teenager** — интерактивное веб-приложение для подростков с геймифицированным обучением и AI-ассистентом Катей. Построено на React + TypeScript + Vite с клиентским AI (Google Gemini).

### Основные роли пользователей:
- **TEEN** (подросток) — игрок, проходит квесты, общается с Катей
- **PARENT** (родитель) — смотрит лекции, отслеживает прогресс
- **CURATOR** (куратор) — управляет группами, видит статистику

---

## 📁 Структура проекта

```
ai-pod/
├── App.tsx                    # Главный компонент с роутингом ролей
├── index.tsx                  # Точка входа React
├── index.html                 # HTML с importmap для CDN модулей
├── types.ts                   # TypeScript типы (User, Task, ChatMessage)
├── constants.ts               # Mock данные (TASKS, MOCK_USER, LECTURES)
├── save_data.json            # Прогресс пользователя (сохраняется через GitHub Actions)
├── vite.config.ts            # Конфигурация Vite + env переменные
│
├── components/
│   ├── TeenDashboard.tsx     # Главная страница подростка с квестами
│   ├── ParentDashboard.tsx   # Страница родителя с лекциями
│   ├── CuratorDashboard.tsx  # CRM для куратора
│   ├── KatyaChat.tsx         # AI чат-бот с анимациями
│   ├── TaskModal.tsx         # Модальное окно с заданиями
│   ├── MeditationView.tsx    # Медитации и звуки для релакса
│   ├── TeenProfile.tsx       # Профиль подростка
│   ├── RoleSelector.tsx      # Dev-инструмент для переключения ролей
│   └── Button.tsx            # UI кнопки
│
└── services/
    └── geminiService.ts      # Обёртка для Google Gemini API
```

---

## 🏗️ Архитектура

### Frontend (React 19 + TypeScript)
- **SPA** (Single Page Application) без роутинга — переключение через условный рендер
- **State**: локальный через `useState` (нет Redux/MobX)
- **Стили**: Tailwind CSS через CDN (`https://cdn.tailwindcss.com`)
- **Иконки**: `lucide-react`

### AI Integration (Google Gemini)
- **Клиент**: `@google/genai` (из CDN `aistudiocdn.com`)
- **API Key**: передаётся через `process.env.GEMINI_API_KEY` (настроено в `vite.config.ts`)
- **Промпты**: в `services/geminiService.ts` → `systemPrompt` (адаптация под интересы подростка)

### Data Persistence
- **save_data.json**: хранит прогресс (XP, completedTaskIds, chatHistory)
- **Cloud Sync**: через GitHub Actions (кнопка "Сохранить прогресс" в AI Studio)
- **Локально**: можно добавить localStorage fallback

---

## 🔑 Ключевые файлы для редактирования

| Файл | Что там | Когда менять |
|------|---------|--------------|
| `services/geminiService.ts` | Промпты AI, настройки модели | Изменить личность Кати, добавить новые функции AI |
| `constants.ts` | TASKS (квесты), MOCK_USER, LECTURES | Добавить новые задания, изменить данные |
| `components/TeenDashboard.tsx` | UI дорожки квестов, SVG градиенты | Изменить дизайн, анимации, расположение |
| `components/KatyaChat.tsx` | Интерфейс чата, анимации персонажа | Стиль чата, реакции, голос Кати |
| `App.tsx` | Роутинг ролей, глобальный стейт | Добавить новые роли, изменить навигацию |
| `vite.config.ts` | Env переменные, HMR настройки | Настроить порты, добавить env vars |
| `index.html` | Importmap для CDN модулей | Обновить версии библиотек |

---

## 🛠️ Developer Workflow

### Локальная разработка
```bash
# 1. Установить зависимости
npm install

# 2. Создать .env.local с API ключом
echo "GEMINI_API_KEY=ваш_ключ_здесь" > .env.local

# 3. Запустить dev-сервер
npm run dev
# Откроется http://localhost:3000/

# 4. Сборка production
npm run build
```

### Синхронизация с AI Studio
```bash
# Подтянуть изменения из AI Studio (GitHub)
git pull origin main

# Посмотреть что изменилось
git status
git diff

# Закоммитить локальные правки
git add .
git commit -m "feat: описание изменений"
git push origin main
```

### Дебаггинг
- **Белый экран** → проверить консоль браузера (F12)
- **Ошибки импорта** → проверить `index.html` (`<script type="module" src="/index.tsx"></script>`)
- **WebSocket ошибки** → открыть в обычном браузере (не Simple Browser VS Code)
- **AI не отвечает** → проверить `process.env.GEMINI_API_KEY` в консоли

---

## 🎨 Особенности дизайна

### Градиентная дорога квестов
```tsx
// components/TeenDashboard.tsx
<svg>
  <defs>
    <linearGradient id="pathGradient">
      <stop offset="0%" stopColor="#6366f1" stopOpacity="0.6" />   {/* indigo */}
      <stop offset="50%" stopColor="#8b5cf6" stopOpacity="0.7" />  {/* violet */}
      <stop offset="100%" stopColor="#a855f7" stopOpacity="0.6" /> {/* purple */}
    </linearGradient>
  </defs>
  <path stroke="url(#pathGradient)" strokeWidth="50" ... />
</svg>
```

### Анимации персонажа Кати
```tsx
// components/KatyaChat.tsx
const KATYA_VARIANTS = {
  IDLE: '...svg?eyes=happy&mouth=smile',
  BLINK: '...svg?eyes=closed&mouth=smile',
  TALK: '...svg?eyes=happy&mouth=default',
  TALK_OPEN: '...svg?eyes=happy&mouth=scream',
}
```

### Адаптация контента под интересы
```tsx
// services/geminiService.ts
const systemPrompt = `
  У пользователя есть интерес: "${userInterest}".
  Если "Гейминг" -> говори про XP, боссов, лут.
  Если "Футбол" -> термины "тайм", "пас", "гол".
  ...
`;
```

---

## 🔧 Конфигурация

### Vite (vite.config.ts)
```typescript
export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, '.', '');
  return {
    server: {
      port: 3000,
      host: '0.0.0.0',
      hmr: { clientPort: 3000, protocol: 'ws' },
    },
    define: {
      'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY)
    }
  };
});
```

### Environment Variables
```bash
# .env.local (НЕ коммитить!)
GEMINI_API_KEY=your_key_here
```

---

## 🚀 Деплой

### AI Studio (автоматический)
1. Кнопка "Сохранить прогресс" → коммит в GitHub
2. AI Studio автоматически деплоит на свой CDN
3. URL: https://ai.studio/apps/drive/...

### Vercel / Netlify (ручной)
```bash
# 1. Билд
npm run build

# 2. Деплой dist/
vercel deploy dist/
# или
netlify deploy --prod --dir=dist
```

⚠️ **Важно**: Не забудьте добавить `GEMINI_API_KEY` в env переменные хостинга!

---

## 🐛 Частые проблемы

### 1. Белый экран
**Причина**: React не загружается  
**Решение**: 
- Проверить `index.html` → должен быть `<script type="module" src="/index.tsx"></script>`
- Открыть консоль (F12) → посмотреть ошибки
- Проверить что `npm run dev` запущен

### 2. WebSocket connection failed
**Причина**: Simple Browser VS Code блокирует WS  
**Решение**: Открыть в обычном браузере (Chrome/Edge)

### 3. AI не отвечает
**Причина**: Нет API ключа  
**Решение**: Проверить `.env.local` и перезапустить `npm run dev`

### 4. Старый дизайн после изменений
**Причина**: AI Studio не закоммитил изменения  
**Решение**: В AI Studio сказать "Сохрани ВСЕ файлы, не только save_data.json"

---

## 📚 Полезные ссылки

- **AI Studio**: https://ai.studio/apps/drive/1bXHHCbf6fYioP9eHGif8NUKO6anr392m
- **GitHub Repo**: https://github.com/johnda7/ai-pod
- **Gemini API Docs**: https://ai.google.dev/docs
- **Vite Docs**: https://vitejs.dev/
- **Tailwind CSS**: https://tailwindcss.com/

---

## 🤝 Вклад для AI-агентов

При работе с этим проектом:
1. **Сохраняй русский язык** в UI и промптах
2. **Тестируй изменения** локально перед коммитом
3. **Обновляй этот файл** при добавлении новых фич
4. **Коммить всё сразу** (не только save_data.json)
5. **Документируй** сложную логику комментариями

---

*Этот файл создан для помощи AI-агентам (Copilot, Claude, ChatGPT) в навигации по проекту.*
