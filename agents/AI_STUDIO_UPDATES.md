# Google AI Studio - Инструкция по обновлениям для AI Агентов

## 🎯 Главный файл для работы с обновлениями от Google AI Studio

**Этот файл обязателен для изучения всеми AI агентами!**

---

## 📍 Где находятся комментарии и обновления от AI Studio

### 1. **В Git истории (основное место)**

Google AI Studio создает коммиты в ветке `main` на GitHub. Все комментарии, описания изменений и предложения находятся в **сообщениях коммитов**.

**Путь к обновлениям:**
```
GitHub Repository: https://github.com/johnda7/ai-pod
Ветка: main
Формат: Git коммиты с описательными сообщениями
```

### 2. **Структура комментариев AI Studio**

AI Studio сохраняет информацию в двух местах коммита:

- **Subject (заголовок)**: Краткое описание изменения
  - Пример: `fix: Adjust header padding for Telegram UI`
  
- **Body (тело)**: Подробное описание изменений
  - Пример: `Increase top padding on dashboard, meditation, and shop views to prevent content overlap with Telegram's persistent UI elements. This ensures a cleaner and more accessible user experience.`

---

## 🔄 Как обновляться от AI Studio

### Автоматический процесс (когда пользователь говорит "обновись")

**ВАЖНО:** Когда пользователь просит обновиться, выполните эти шаги **автоматически**:

```bash
# 1. Получить последние изменения с GitHub
git fetch origin

# 2. Проверить, есть ли новые коммиты
git log HEAD..origin/main --oneline

# 3. Если есть новые коммиты - подтянуть их
git pull origin main

# 4. Показать пользователю, что обновилось
git log HEAD~5..HEAD --format="%h - %s%n%b" --oneline
```

### Полная команда одной строкой:

```bash
git fetch origin && git pull origin main && git log -5 --format="%h - %s%n%b"
```

---

## 📋 Пошаговая инструкция для агентов

### Шаг 1: Проверка обновлений

```bash
cd /Users/evgeniymikhelev/Documents/GitHub/ai-pod/ai-pod
git fetch origin
```

### Шаг 2: Сравнение с локальной версией

```bash
# Посмотреть, какие коммиты есть на GitHub, но нет локально
git log HEAD..origin/main --oneline

# Если вывод не пустой - есть обновления!
```

### Шаг 3: Получение обновлений

```bash
# Подтянуть все новые коммиты
git pull origin main
```

### Шаг 4: Просмотр изменений

```bash
# Посмотреть последние 5 коммитов с описаниями
git log -5 --format="%h - %s%n%b"

# Или посмотреть конкретный коммит
git show HEAD
```

---

## 🔍 Как найти комментарии AI Studio в коммитах

### Метод 1: Поиск по сообщениям коммитов

```bash
# Найти все коммиты с определенным текстом
git log --all --grep="fix\|feat\|refactor" --oneline

# Найти коммиты от AI Studio (обычно имеют описательные сообщения)
git log --format="%h|%s|%b" --all | grep -i "adjust\|update\|improve\|add\|fix"
```

### Метод 2: Просмотр последних коммитов

```bash
# Последние 10 коммитов с полными описаниями
git log -10 --format="%h - %s%n%b%n---"

# Только коммиты с подробными описаниями (body не пустой)
git log --format="%h|%s|%b" | awk -F'|' 'length($3) > 0'
```

### Метод 3: Извлечение всех комментариев в файл

```bash
# Создать файл со всеми комментариями
git log --format="## %h - %s%n%n**Дата:** %ai%n%n%b%n---%n" --all > ai-studio-comments.md
```

---

## 📝 Формат комментариев AI Studio

AI Studio использует стандартный формат Conventional Commits:

### Типы коммитов:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `refactor:` - рефакторинг кода
- `docs:` - документация
- `style:` - форматирование
- `test:` - тесты

### Примеры реальных коммитов от AI Studio:

```
9034b33 - fix: Adjust header padding for Telegram UI
Body: Increase top padding on dashboard, meditation, and shop views 
to prevent content overlap with Telegram's persistent UI elements.

3ff5d1c - feat: Add 'Anatomy of Laziness' task
Body: Introduces a new task for Week 2 focused on understanding 
and overcoming laziness. This task includes theory slides, a sorting 
activity, a mini-game, and a puzzle.

7b54806 - refactor: Adjust spacing and styling in dashboard views
Body: Updates padding and visual elements in TeenDashboard, 
MeditationView, ShopView, and LeaderboardView for improved layout.
```

---

## 🚀 Автоматизация для агентов

### Когда пользователь говорит "обновись" или "обновись":

**Выполните автоматически:**

1. ✅ `git fetch origin` - получить информацию о новых коммитах
2. ✅ `git log HEAD..origin/main --oneline` - проверить наличие обновлений
3. ✅ Если есть обновления → `git pull origin main`
4. ✅ `git log -5 --format="%h - %s%n%b"` - показать что обновилось
5. ✅ Сообщить пользователю о новых изменениях

### Скрипт для автоматического обновления:

```bash
#!/bin/bash
# auto-update-from-ai-studio.sh

echo "🔄 Проверка обновлений от AI Studio..."

# Получить обновления
git fetch origin

# Проверить наличие новых коммитов
NEW_COMMITS=$(git log HEAD..origin/main --oneline)

if [ -z "$NEW_COMMITS" ]; then
    echo "✅ Проект уже обновлен до последней версии"
else
    echo "📥 Найдены новые коммиты от AI Studio:"
    echo "$NEW_COMMITS"
    echo ""
    echo "⬇️  Загружаю обновления..."
    git pull origin main
    echo ""
    echo "✅ Обновление завершено!"
    echo ""
    echo "📋 Последние изменения:"
    git log -5 --format="%h - %s%n%b" --oneline
fi
```

---

## 📊 Как видеть обновления

### В терминале:

```bash
# Компактный вид
git log --oneline -10

# С описаниями
git log --format="%h - %s%n%b" -10

# С датами
git log --format="%h - %s (%ai)%n%b" -10
```

### На GitHub:

1. Откройте: https://github.com/johnda7/ai-pod
2. Перейдите в раздел "Commits"
3. Каждый коммит содержит:
   - Заголовок (subject)
   - Описание (body) - это и есть комментарии AI Studio
   - Измененные файлы

### В локальном проекте:

```bash
# Посмотреть конкретный коммит
git show <commit-hash>

# Посмотреть изменения в файлах
git show <commit-hash> --stat

# Посмотреть diff
git show <commit-hash>
```

---

## 🎯 Применение обновлений в проекте

### После получения обновлений:

1. **Проверьте измененные файлы:**
   ```bash
   git show HEAD --name-only
   ```

2. **Посмотрите diff (что именно изменилось):**
   ```bash
   git show HEAD
   ```

3. **Если нужно откатить изменения:**
   ```bash
   git reset --hard HEAD~1  # Откатить последний коммит
   ```

4. **Если нужно применить только часть изменений:**
   ```bash
   git show HEAD > changes.patch
   # Отредактировать patch файл
   git apply changes.patch
   ```

---

## ⚠️ Важные правила для агентов

### ✅ ОБЯЗАТЕЛЬНО делать:

1. **Всегда проверять обновления перед работой:**
   ```bash
   git fetch origin
   git status
   ```

2. **При запросе "обновись" - выполнять автоматически:**
   - Не спрашивать подтверждения
   - Не искать вручную
   - Использовать команды выше

3. **Показывать пользователю что обновилось:**
   - Список новых коммитов
   - Описания изменений
   - Измененные файлы

### ❌ НЕ делать:

1. ❌ Не искать комментарии в Issues или Pull Requests (они в коммитах!)
2. ❌ Не спрашивать пользователя, как обновляться (используйте эту инструкцию)
3. ❌ Не пропускать шаг проверки обновлений

---

## 📚 Связанные файлы

- `agents/README.md` - общая информация о проекте
- `agents/ARCHITECTURE.md` - архитектура системы
- `agents/API.md` - API и сервисы
- `ai-studio-comments.md` - извлеченные комментарии (генерируется автоматически)

---

## 🔗 Полезные команды (шпаргалка)

```bash
# Быстрая проверка обновлений
git fetch origin && git log HEAD..origin/main --oneline

# Получить обновления
git pull origin main

# Посмотреть последние коммиты с описаниями
git log -5 --format="%h - %s%n%b"

# Посмотреть конкретный коммит
git show <hash>

# Извлечь все комментарии в файл
git log --format="## %h - %s%n%n**Дата:** %ai%n%n%b%n---%n" --all > ai-studio-comments.md
```

---

## 💡 Примеры использования

### Пример 1: Пользователь говорит "обновись"

```bash
# Агент автоматически выполняет:
git fetch origin
git pull origin main
git log -5 --format="%h - %s%n%b"

# И сообщает:
"✅ Проект обновлен! Получено 2 новых коммита от AI Studio:
- 9034b33 - fix: Adjust header padding for Telegram UI
- 3ff5d1c - feat: Add 'Anatomy of Laziness' task"
```

### Пример 2: Проверка перед работой

```bash
# Агент проверяет:
git fetch origin
git log HEAD..origin/main --oneline

# Если есть обновления:
"⚠️ Обнаружены новые коммиты от AI Studio. Рекомендую обновиться."
```

---

## 🎓 Для новых агентов

**Если вы впервые работаете с этим проектом:**

1. Прочитайте этот файл полностью
2. Запомните команды обновления
3. При запросе "обновись" - используйте автоматический процесс
4. Не ищите комментарии в других местах - они в Git истории!

---

**Последнее обновление инструкции:** 2025-11-26  
**Версия:** 1.0

















