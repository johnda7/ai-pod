# Работа с Google AI Studio

## 🎯 Полное руководство по работе с AI Studio для агентов

---

## 📍 Где находятся комментарии и изменения от AI Studio

### **В Git коммитах на GitHub**

**Важно:** Все комментарии, описания изменений и предложения от AI Studio находятся в **сообщениях коммитов Git** на GitHub.

**Путь:**
- Репозиторий: `https://github.com/johnda7/ai-pod`
- Ветка: `main`
- Формат: Git коммиты с описательными сообщениями

---

## 🔄 Автоматическое получение коммитов

### Когда пользователь говорит "покажи логи":

**Агент автоматически выполняет:**

```bash
# 1. Получить последние коммиты с GitHub
git fetch origin

# 2. Показать последние коммиты с полными описаниями
git log origin/main -10 --format="## %h - %s%n%n**Дата:** %ai%n%n%b%n---%n"
```

### Когда пользователь говорит "обновись":

**Агент автоматически выполняет:**

```bash
# 1. Получить информацию о новых коммитах
git fetch origin

# 2. Проверить наличие обновлений
git log HEAD..origin/main --oneline

# 3. Если есть - подтянуть
git pull origin main

# 4. Показать что обновилось
git log -5 --format="%h - %s%n%b"
```

---

## 📋 Структура комментариев AI Studio

### Формат коммитов:

AI Studio использует стандартный формат Conventional Commits:

```
<type>: <subject>

<body>
```

### Типы коммитов:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `refactor:` - рефакторинг кода
- `docs:` - документация
- `style:` - форматирование

### Где находятся комментарии:

1. **Subject (заголовок)** - краткое описание
   - Пример: `fix: Adjust header padding for Telegram UI`

2. **Body (тело)** - подробное описание изменений
   - Пример: `Increase top padding on dashboard, meditation, and shop views to prevent content overlap with Telegram's persistent UI elements. This ensures a cleaner and more accessible user experience.`

**Комментарии AI Studio находятся в Body коммитов!**

---

## 🚀 Команды для агентов

### Получить и показать логи:

```bash
# Получить последние коммиты с GitHub
git fetch origin

# Показать последние 10 коммитов с описаниями
git log origin/main -10 --format="%h - %s%n%b"

# Показать только коммиты с подробными описаниями (body не пустой)
git log origin/main --format="%h|%s|%b" | awk -F'|' 'length($3) > 0'
```

### Обновить проект:

```bash
# Получить обновления
git fetch origin && git pull origin main

# Показать что обновилось
git log HEAD~5..HEAD --format="%h - %s%n%b"
```

### Извлечь все комментарии в файл:

```bash
# Создать файл со всеми комментариями
git log origin/main --format="## %h - %s%n%n**Дата:** %ai%n%n%b%n---%n" > ai-studio-comments.md
```

---

## 🔍 Как найти конкретные комментарии

### Поиск по тексту:

```bash
# Найти коммиты с определенным текстом
git log origin/main --grep="fix\|feat\|refactor" --oneline

# Найти коммиты с описаниями (body не пустой)
git log origin/main --format="%h|%s|%b" | awk -F'|' 'length($3) > 0 {print $1" - "$2}'
```

### Просмотр конкретного коммита:

```bash
# Посмотреть полный коммит с описанием
git show <commit-hash>

# Только сообщение коммита
git log <commit-hash> -1 --format="%s%n%n%b"
```

---

## 📊 Примеры реальных коммитов от AI Studio

### Пример 1: Исправление бага

```
9034b33 - fix: Adjust header padding for Telegram UI

Increase top padding on dashboard, meditation, and shop views 
to prevent content overlap with Telegram's persistent UI elements. 
This ensures a cleaner and more accessible user experience.
```

### Пример 2: Новая функциональность

```
3ff5d1c - feat: Add 'Anatomy of Laziness' task

Introduces a new task for Week 2 focused on understanding 
and overcoming laziness. This task includes theory slides, 
a sorting activity, a mini-game, and a puzzle to help users 
identify and combat procrastination triggers.
```

### Пример 3: Рефакторинг

```
7b54806 - refactor: Adjust spacing and styling in dashboard views

Updates padding and visual elements in TeenDashboard, 
MeditationView, ShopView, and LeaderboardView for improved 
layout and aesthetics. Minor icon updates also included.
```

---

## ⚡ Автоматизация для агентов

### Скрипт для автоматического получения логов:

```bash
#!/bin/bash
# get-ai-studio-logs.sh

echo "📥 Получение коммитов от AI Studio с GitHub..."

# Получить последние коммиты
git fetch origin

# Показать последние 10 коммитов с описаниями
echo ""
echo "📋 Последние коммиты от AI Studio:"
echo ""
git log origin/main -10 --format="## %h - %s%n%n**Дата:** %ai%n%n%b%n---%n"
```

### Использование:

```bash
# Когда пользователь говорит "покажи логи"
bash get-ai-studio-logs.sh
```

---

## 🎯 Правила для агентов

### ✅ ОБЯЗАТЕЛЬНО делать:

1. **При запросе "покажи логи":**
   - Автоматически выполнять `git fetch origin`
   - Показывать коммиты из `origin/main` (не локальные!)
   - Показывать полные описания (body)

2. **При запросе "обновись":**
   - Автоматически выполнять `git pull origin main`
   - Показывать что обновилось
   - Не спрашивать подтверждения

3. **Всегда получать с GitHub:**
   - Использовать `origin/main`, а не локальный `HEAD`
   - Делать `git fetch` перед просмотром логов

### ❌ НЕ делать:

1. ❌ Не искать комментарии в Issues или Pull Requests
2. ❌ Не спрашивать, как получать логи
3. ❌ Не показывать только локальные коммиты
4. ❌ Не пропускать `git fetch origin`

---

## 📝 Workflow работы с AI Studio

### Полный цикл:

```
1. AI Studio создает код
   ↓
2. AI Studio делает коммит на GitHub
   (коммит содержит комментарии в body)
   ↓
3. Пользователь говорит "покажи логи"
   ↓
4. Агент выполняет:
   git fetch origin
   git log origin/main -10 --format="..."
   ↓
5. Показывает коммиты с комментариями
```

### Обновление проекта:

```
1. Пользователь говорит "обновись"
   ↓
2. Агент выполняет:
   git fetch origin
   git pull origin main
   ↓
3. Показывает что обновилось
```

---

## 🔗 Связанные файлы

- `AGENT_MD.md` - главный файл с общим workflow
- `../agents/AI_STUDIO_UPDATES.md` - детальная инструкция по обновлениям
- `LOCAL_WORK.md` - локальная работа с проектом

---

## 💡 Быстрая справка

```bash
# Показать логи (автоматически при запросе)
git fetch origin && git log origin/main -10 --format="%h - %s%n%b"

# Обновить проект (автоматически при запросе)
git fetch origin && git pull origin main && git log -5 --format="%h - %s%n%b"

# Извлечь все комментарии
git log origin/main --format="## %h - %s%n%n%b%n---%n" > comments.md
```

---

**Версия:** 1.0  
**Последнее обновление:** 2025-11-26

