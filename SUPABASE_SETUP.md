# 🔧 Настройка Supabase для AI Pod

## 📋 Что нужно для настройки

1. **Supabase URL** - URL вашего проекта (например: `https://xxxxx.supabase.co`)
2. **Supabase Anon Key** - Публичный API ключ (anon key)

## 🚀 Шаги настройки

### Шаг 1: Получите данные из Supabase Dashboard

1. Зайдите на [supabase.com](https://supabase.com)
2. Откройте ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка начинающаяся с `eyJ...`)

### Шаг 2: Создайте таблицы в Supabase

1. В Supabase Dashboard перейдите в **SQL Editor**
2. Откройте файл `scripts/setup-supabase.sql`
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor и нажмите **Run**
5. Должно появиться сообщение об успешном выполнении

### Шаг 3: Обновите ключи в коде

Откройте файл `services/supabaseClient.ts` и замените:

```typescript
const SUPABASE_URL = 'ВАШ_URL_ЗДЕСЬ';
const SUPABASE_KEY = 'ВАШ_КЛЮЧ_ЗДЕСЬ';
```

### Шаг 4: Проверьте подключение

Запустите тестовый скрипт:

```bash
# Установите зависимости если нужно
npm install

# Запустите тест (укажите ваши ключи)
SUPABASE_URL=ваш_url SUPABASE_KEY=ваш_ключ npx tsx scripts/test-supabase.ts
```

Или обновите ключи в `scripts/test-supabase.ts` и запустите:

```bash
npx tsx scripts/test-supabase.ts
```

### Шаг 5: Запустите приложение

```bash
npm run dev
```

Откройте консоль браузера (F12) и проверьте:
- Должно быть сообщение: `✅ Supabase успешно подключена: ваш_url`
- При входе через Telegram должны загружаться данные из БД

## ✅ Проверка работы

1. **Войдите через Telegram** - данные должны загрузиться из Supabase
2. **Завершите задачу** - проверьте консоль на сообщение `✅ Task completed and synced to Supabase`
3. **Купите предмет** - проверьте консоль на сообщение `✅ Purchase synced to Supabase`
4. **Выйдите и войдите снова** - прогресс должен сохраниться

## 🔍 Проверка структуры таблиц

В SQL Editor выполните скрипт из `scripts/check-supabase-schema.sql` для проверки структуры.

## 🐛 Решение проблем

### Ошибка: "relation 'users' does not exist"
- Выполните SQL скрипт `scripts/setup-supabase.sql`

### Ошибка: "new row violates row-level security policy"
- Проверьте политики RLS в SQL Editor
- Убедитесь, что политики "Allow all" созданы

### Данные не сохраняются
- Проверьте консоль браузера на ошибки
- Убедитесь, что ключи правильные
- Проверьте, что таблицы созданы

### Пользователь не находится
- Проверьте, что `telegram_id` правильно сохраняется
- Проверьте логи в консоли: `DB: Checking Supabase for Telegram ID...`

## 📝 Структура таблиц

### Таблица `users`
- `id` (UUID) - уникальный ID
- `telegram_id` (BIGINT) - ID из Telegram (уникальный)
- `name`, `username` - данные пользователя
- `xp`, `coins`, `level` - игровые данные
- `hp`, `max_hp` - здоровье
- `inventory` (JSONB) - инвентарь
- `completedTaskIds` - загружается из таблицы `progress`

### Таблица `progress`
- `id` (UUID) - уникальный ID
- `user_id` (UUID) - ссылка на users.id
- `task_id` (TEXT) - ID задачи
- `xp_earned` - полученный XP
- `completed_at` - дата завершения

## 🔐 Безопасность

⚠️ **Важно**: Текущие политики RLS разрешают все операции для анонимных пользователей.

Для продакшена рекомендуется:
1. Настроить более строгие политики RLS
2. Использовать сервисный ключ (service_role) на бэкенде
3. Добавить проверку авторизации через Telegram
