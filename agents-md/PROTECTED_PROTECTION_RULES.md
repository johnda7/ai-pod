# 🛡️ ПРАВИЛА ЗАЩИТЫ КРИТИЧЕСКИ ВАЖНЫХ ФАЙЛОВ

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ЗАПРЕТ НА УДАЛЕНИЕ

### Файлы, которые НИКОГДА нельзя удалять:

1. **Все файлы в папке `agents-md/`** - главная документация для агентов
2. **Все файлы в папке `agents/`** - дополнительная документация
3. **Любые `.md` файлы** в корне проекта (кроме временных)
4. **Файлы конфигурации:**
   - `package.json`
   - `vite.config.ts`
   - `tsconfig.json`
   - `.github/workflows/*.yml`

### Почему это критично:

- **Документация для агентов** - без неё агенты не смогут работать с проектом
- **История проекта** - MD файлы содержат важную информацию
- **Инструкции** - они нужны для понимания workflow

---

## 🚫 ЧТО ПРОИЗОШЛО (история проблемы)

### Проблема:
В коммите `a24f9ba` были удалены все файлы из папок `agents-md/` и `agents/`. Это произошло при обновлении из GitHub (`git pull`).

### Причина:
- AI Studio или другой разработчик удалил файлы в коммите
- При `git pull` эти удаления применились локально
- Файлы были восстановлены из истории Git (коммит `1521514`)

### Решение:
- Файлы восстановлены из истории Git
- Добавлены правила защиты
- Создан скрипт проверки перед pull

---

## ✅ ПРАВИЛА ДЛЯ АГЕНТОВ

### 1. ПЕРЕД ВЫПОЛНЕНИЕМ `git pull`:

**ВСЕГДА проверяйте, что будет удалено:**

```bash
# Проверить, какие файлы будут удалены при pull
git fetch origin
git diff --name-status HEAD..origin/main | grep "^D" | grep -E "\.(md|MD)$|agents-md|agents/"
```

**Если видите удаление MD файлов или файлов из agents/agents-md:**
- ⛔ **НЕ ВЫПОЛНЯЙТЕ `git pull`**
- 📝 Сообщите пользователю
- 🔍 Проверьте коммиты: `git log origin/main --oneline -5`
- 💬 Спросите разрешение на восстановление

### 2. ПРИ ОБНАРУЖЕНИИ УДАЛЕНИЯ:

```bash
# 1. НЕ делайте pull
# 2. Найдите коммит, где файлы ещё были
git log --all --full-history --oneline -- "agents-md/" "agents/"

# 3. Восстановите файлы
git checkout <commit-hash> -- agents-md/ agents/

# 4. Сообщите пользователю
```

### 3. ПРИ СОЗДАНИИ КОММИТОВ:

**НИКОГДА не удаляйте:**
- ❌ Файлы из `agents-md/`
- ❌ Файлы из `agents/`
- ❌ Любые `.md` файлы (кроме явно временных)
- ❌ Конфигурационные файлы

**Если нужно обновить документацию:**
- ✅ Редактируйте существующие файлы
- ✅ Добавляйте новые файлы
- ✅ НЕ удаляйте старые без явного разрешения

---

## 🔧 АВТОМАТИЧЕСКАЯ ЗАЩИТА

### Скрипт проверки перед pull:

Создан скрипт `scripts/check-before-pull.sh`, который:
1. Проверяет, какие файлы будут удалены
2. Предупреждает о удалении MD файлов
3. Блокирует pull при обнаружении критичных удалений

**Использование:**
```bash
./scripts/check-before-pull.sh
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД ОБНОВЛЕНИЕМ

- [ ] Выполнить `git fetch origin`
- [ ] Проверить удаления: `git diff --name-status HEAD..origin/main | grep "^D"`
- [ ] Если есть удаления MD файлов - НЕ делать pull
- [ ] Если удалений нет - выполнить `git pull`
- [ ] После pull проверить наличие папок `agents-md/` и `agents/`
- [ ] Если папок нет - восстановить из истории

---

## 🆘 ВОССТАНОВЛЕНИЕ ФАЙЛОВ

Если файлы были удалены:

```bash
# 1. Найти коммит, где файлы были
git log --all --full-history --oneline -- "agents-md/" "agents/"

# 2. Восстановить из конкретного коммита
git checkout <commit-hash> -- agents-md/ agents/

# 3. Проверить восстановление
ls -la agents-md/ agents/

# 4. Закоммитить восстановление
git add agents-md/ agents/
git commit -m "restore: Recover critical documentation files"
```

---

## 📝 ДЛЯ AI STUDIO И ДРУГИХ РАЗРАБОТЧИКОВ

**ВАЖНО:** При работе в AI Studio или других инструментах:

1. **НЕ удаляйте** папки `agents-md/` и `agents/`
2. **НЕ удаляйте** любые `.md` файлы
3. **Если нужно обновить** - редактируйте, не удаляйте
4. **Перед коммитом** проверьте, что не удаляете критичные файлы

---

## 🎯 ИТОГО

**ГЛАВНОЕ ПРАВИЛО:**
> **MD файлы и документация в `agents-md/` и `agents/` - это КРИТИЧЕСКИ ВАЖНЫЕ файлы. Они НИКОГДА не должны удаляться. Если они удалены - их нужно немедленно восстановить из истории Git.**

---

**Версия:** 1.0  
**Создано:** 2025-11-26  
**Причина:** Восстановление после случайного удаления в коммите a24f9ba


