# Локальная работа с проектом AI Pod

## 🎯 Для AI Агентов - полная инструкция по локальной работе

---

## 📋 Требования

### Обязательные:
- **Node.js** 18+ (проверить: `node --version`)
- **npm** (устанавливается с Node.js)
- **Git** (проверить: `git --version`)

### Опциональные (для полного функционала):
- **GEMINI_API_KEY** - для AI-наставника Катя
- **Supabase** ключи - для хранения данных
- **Telegram Bot Token** - для Telegram бота

---

## 🚀 Первоначальная настройка

### Шаг 1: Клонирование/обновление проекта

```bash
# Если проект еще не клонирован
git clone https://github.com/johnda7/ai-pod.git
cd ai-pod

# Если проект уже есть - обновите его
cd /Users/evgeniymikhelev/Documents/GitHub/ai-pod/ai-pod
git fetch origin
git pull origin main
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

**Что устанавливается:**
- React 19
- TypeScript
- Vite
- Google Gemini API
- Supabase
- Lucide React (иконки)
- И другие зависимости

### Шаг 3: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# .env.local
GEMINI_API_KEY=ваш_ключ_здесь
```

**Где взять ключ:**
- Google AI Studio: https://aistudio.google.com/
- Создайте API ключ для Gemini

---

## 🏃 Запуск проекта локально

### Основная команда:

```bash
npm run dev
```

**Результат:**
- Сервер запустится на `http://localhost:3000`
- Автоматическая перезагрузка при изменениях
- Hot Module Replacement (HMR)

### Проверка работы:

1. Откройте браузер: `http://localhost:3000`
2. Должна появиться загрузочная анимация (Boot Sequence)
3. Затем откроется дашборд для подростков

---

## 🛠️ Доступные команды

### Разработка:

```bash
# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранной версии
npm run preview
```

### Git команды:

```bash
# Обновление от AI Studio
git fetch origin && git pull origin main

# Проверка статуса
git status

# Просмотр изменений
git diff

# Просмотр истории
git log --oneline -10
```

---

## 📁 Структура проекта для работы

### Основные папки:

```
ai-pod/
├── components/          # React компоненты UI
│   ├── TeenDashboard.tsx    # Главный дашборд
│   ├── TaskModal.tsx        # Модальное окно урока
│   ├── KatyaChat.tsx        # Чат с AI
│   └── ...
│
├── services/           # Бизнес-логика
│   ├── db.ts               # Работа с данными
│   ├── geminiService.ts    # AI-наставник
│   ├── telegramService.ts  # Telegram интеграция
│   └── ...
│
├── types.ts            # TypeScript типы
├── constants.ts        # Данные (задачи, пользователи)
└── App.tsx             # Главный компонент
```

### Где что редактировать:

- **Новые компоненты** → `components/`
- **Новая логика** → `services/`
- **Новые типы** → `types.ts`
- **Новые данные** → `constants.ts`
- **Главная логика** → `App.tsx`

---

## 🔧 Работа с кодом

### Добавление нового компонента:

1. Создайте файл в `components/`:
   ```typescript
   // components/MyComponent.tsx
   import React from 'react';
   
   export const MyComponent: React.FC = () => {
     return <div>Мой компонент</div>;
   };
   ```

2. Импортируйте в нужном месте:
   ```typescript
   import { MyComponent } from './components/MyComponent';
   ```

### Изменение данных:

1. Откройте `constants.ts`
2. Найдите нужный массив (TASKS, SHOP_ITEMS, и т.д.)
3. Добавьте/измените данные

### Работа с типами:

1. Откройте `types.ts`
2. Добавьте/измените интерфейсы
3. Используйте в компонентах

---

## 🧪 Тестирование

### Локальное тестирование:

1. Запустите `npm run dev`
2. Откройте `http://localhost:3000`
3. Проверьте функциональность вручную

### Проверка сборки:

```bash
# Собрать проект
npm run build

# Проверить собранную версию
npm run preview
```

**Ожидаемый результат:**
- Сборка без ошибок
- Файлы в папке `dist/`
- Приложение работает на preview сервере

---

## 🐛 Решение проблем

### Проблема: Зависимости не устанавливаются

```bash
# Очистить кэш и переустановить
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Порт 3000 занят

```bash
# Изменить порт в vite.config.ts
# Или убить процесс на порту 3000
lsof -ti:3000 | xargs kill -9
```

### Проблема: Ошибки TypeScript

```bash
# Проверить ошибки
npm run build

# Или через tsc
npx tsc --noEmit
```

### Проблема: AI не работает

- Проверьте `.env.local` файл
- Убедитесь, что `GEMINI_API_KEY` установлен
- Проверьте интернет-соединение

---

## 📝 Работа с Git

### Перед началом работы:

```bash
# Всегда обновляйтесь перед работой
git fetch origin
git pull origin main
```

### После изменений:

```bash
# Проверить что изменилось
git status

# Посмотреть изменения
git diff

# Если нужно закоммитить (обычно не нужно - AI Studio делает коммиты)
git add .
git commit -m "Описание изменений"
git push origin main
```

**Важно:** Обычно коммиты делает AI Studio, а не агенты в VS Code!

---

## 🔄 Синхронизация с AI Studio

### Получение обновлений:

```bash
# Автоматический процесс (когда пользователь говорит "обновись")
git fetch origin
git pull origin main
git log -5 --format="%h - %s%n%b"
```

### Проверка новых коммитов:

```bash
# Посмотреть что есть на GitHub, но нет локально
git log HEAD..origin/main --oneline

# Если вывод не пустой - есть обновления!
```

---

## 💻 Работа в VS Code

### Рекомендуемые расширения:

- **ES7+ React/Redux/React-Native snippets**
- **TypeScript and JavaScript Language Features**
- **Tailwind CSS IntelliSense**
- **GitLens**

### Полезные команды VS Code:

- `Cmd+P` - быстрый поиск файлов
- `Cmd+Shift+P` - команды палитры
- `Cmd+B` - показать/скрыть sidebar
- `Cmd+\` - разделить редактор

---

## 🎯 Чеклист для агентов

### Перед началом работы:

- [ ] Обновить проект: `git pull origin main`
- [ ] Проверить зависимости: `npm install`
- [ ] Проверить `.env.local` (если нужен AI)
- [ ] Запустить dev сервер: `npm run dev`

### Во время работы:

- [ ] Сохранять файлы регулярно
- [ ] Проверять консоль на ошибки
- [ ] Тестировать изменения в браузере

### После работы:

- [ ] Проверить что все работает
- [ ] Если нужно - закоммитить изменения
- [ ] Сообщить пользователю о результатах

---

## 📚 Дополнительная информация

- **Архитектура:** `agents/ARCHITECTURE.md`
- **API:** `agents/API.md`
- **Модели данных:** `agents/DATA_MODELS.md`
- **Обновления:** `agents/AI_STUDIO_UPDATES.md`

---

**Версия:** 1.0  
**Последнее обновление:** 2025-11-26


